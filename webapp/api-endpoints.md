# API Endpoint Documentation

> Baseline reference for the React web app team consuming the `exam-generator` REST API.

## Authentication

Every request must include the deployment-specific API key in the `x-api-key` header:

```http
x-api-key: {EXAM_GENERATOR_API_KEY}
```

The API uses API Gateway request authorization with CORS enabled. There is no user/session authentication; the key is shared by the web app.

## Base URL and versioning

The API is served from an API Gateway stage. Example production URL format:

```
https://{api-id}.execute-api.{region}.amazonaws.com/{stage}
```

All endpoints are prefixed with `/v1`.

Example:

```
GET https://api.example.com/v1/certifications
```

All requests and responses use `Content-Type: application/json` unless otherwise noted.

## Error response format

All error responses share this JSON shape:

```json
{
  "error": "CamelCaseErrorCode",
  "message": "Human-readable description"
}
```

| Error code | HTTP status | Typical cause |
| --- | --- | --- |
| `InvalidRequest` | 400 | Malformed body, invalid query params, or invalid config values. |
| `NotFound` | 404 | Resource does not exist. |
| `ExamNotFound` | 404 | Exam-specific lookup failed. |
| `ExamNotReady` | 409 | Exam exists but is not in `READY` status. |
| `Conflict` | 409 | Duplicate `provider` + `code` or immutable fields changed. |
| `RateLimitExceeded` | 429 | Too many requests from the same IP. |
| `InternalError` | 500 | Unexpected server error. |

## Endpoints

### Health check

#### `GET /v1/health`

Simple liveness check.

**Response — 200 OK**

```json
{
  "status": "ok"
}
```

---

### Create certification

#### `POST /v1/certifications`

Creates a new certification catalog entry. The `id` is generated by the backend. The response omits the internal `promptTemplate` from `config`.

**Request body**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `provider` | `string` enum (`aws`, `azure`, `gcp`) | yes | Immutable after creation. |
| `code` | `string` | yes | Provider-specific exam code. Immutable after creation. |
| `name` | `string` | yes | Human-readable title. |
| `version` | `string` | yes | Free-form version metadata. |
| `description` | `string` | yes | Short description. |
| `isActive` | `boolean` | yes | Whether it appears in public listings. |
| `config.questionCount` | `integer` | yes | `1`–`100`. |
| `config.difficultyDistribution` | `object` | yes | `easy`, `medium`, `hard` numbers that sum to `1.0`. |
| `config.domains` | `string[]` | yes | At least one domain. |
| `config.modelId` | `string` | yes | Bedrock model identifier. |
| `config.promptTemplate` | `string` | yes | LLM prompt template (internal, not returned). |

**Example request**

```json
{
  "provider": "aws",
  "code": "CLF-C02",
  "name": "AWS Certified Cloud Practitioner",
  "version": "v1",
  "description": "Foundational AWS cloud certification.",
  "isActive": true,
  "config": {
    "questionCount": 30,
    "difficultyDistribution": { "easy": 0.2, "medium": 0.5, "hard": 0.3 },
    "domains": ["Cloud Concepts", "Security and Compliance", "Technology", "Billing and Pricing"],
    "modelId": "anthropic.claude-3-haiku-20240307-v1:0",
    "promptTemplate": "Generate a {difficulty} multiple-choice question about {domain} for the {code} exam."
  }
}
```

**Response — 201 Created**

```json
{
  "id": "11111111-1111-1111-1111-111111111111",
  "provider": "aws",
  "code": "CLF-C02",
  "name": "AWS Certified Cloud Practitioner",
  "version": "v1",
  "description": "Foundational AWS cloud certification.",
  "isActive": true,
  "config": {
    "questionCount": 30,
    "difficultyDistribution": { "easy": 0.2, "medium": 0.5, "hard": 0.3 },
    "domains": ["Cloud Concepts", "Security and Compliance", "Technology", "Billing and Pricing"],
    "modelId": "anthropic.claude-3-haiku-20240307-v1:0"
  }
}
```

**Errors**: `400 InvalidRequest`, `409 Conflict` (duplicate `provider` + `code`).

---

### List active certifications

#### `GET /v1/certifications`

Returns all active certifications (`isActive: true`). The response omits `promptTemplate` from each `config`.

**Response — 200 OK**

```json
{
  "items": [
    {
      "id": "11111111-1111-1111-1111-111111111111",
      "provider": "aws",
      "code": "CLF-C02",
      "name": "AWS Certified Cloud Practitioner",
      "version": "v1",
      "description": "Foundational AWS cloud certification.",
      "isActive": true,
      "config": {
        "questionCount": 30,
        "difficultyDistribution": { "easy": 0.2, "medium": 0.5, "hard": 0.3 },
        "domains": ["Cloud Concepts", "Security and Compliance", "Technology", "Billing and Pricing"],
        "modelId": "anthropic.claude-3-haiku-20240307-v1:0"
      }
    }
  ]
}
```

---

### Get a certification

#### `GET /v1/certifications/{id}`

Returns a single certification by UUID. The response omits `promptTemplate`.

**Response — 200 OK**

Same shape as a single certification item from the list response.

**Errors**: `400 InvalidRequest` (bad UUID), `404 NotFound`.

---

### Update a certification

#### `PUT /v1/certifications/{id}`

Updates certification metadata. `provider` and `code` are immutable and must not be sent. The response omits `promptTemplate`.

**Request body**

Same shape as the create request, but without `provider` and `code`. All fields are still required.

**Example request**

```json
{
  "name": "AWS Certified Cloud Practitioner (Updated)",
  "version": "v2",
  "description": "Updated description.",
  "isActive": true,
  "config": {
    "questionCount": 40,
    "difficultyDistribution": { "easy": 0.2, "medium": 0.5, "hard": 0.3 },
    "domains": ["Cloud Concepts", "Security and Compliance", "Technology", "Billing and Pricing"],
    "modelId": "anthropic.claude-3-haiku-20240307-v1:0",
    "promptTemplate": "Generate a {difficulty} multiple-choice question about {domain} for the {code} exam."
  }
}
```

**Response — 200 OK**

Same certification shape as `POST /v1/certifications`.

**Errors**: `400 InvalidRequest` (missing fields or immutable fields present), `404 NotFound`.

---

### Request exam generation

#### `POST /v1/exams`

Starts an asynchronous exam generation job. Returns the exam ID and initial `GENERATING` status.

**Request body**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `certificationId` | `string` (UUID) | yes | Must be an active certification. |

**Example request**

```json
{
  "certificationId": "11111111-1111-1111-1111-111111111111"
}
```

**Response — 201 Created**

```json
{
  "id": "22222222-2222-2222-2222-222222222222",
  "status": "GENERATING"
}
```

**Errors**: `400 InvalidRequest` (missing or invalid UUID), `404 NotFound` (certification unknown or inactive).

---

### List exams

#### `GET /v1/exams`

Returns exam metadata rows. Defaults to `status=READY`.

**Query parameters**

| Parameter | Type | Default | Notes |
| --- | --- | --- | --- |
| `status` | `GENERATING`, `READY`, `FAILED` | `READY` | Filter by status. |
| `provider` | `aws`, `azure`, `gcp` | — | Filter by provider. |
| `certificationId` | UUID | — | Filter by certification. |
| `limit` | `integer` | `20` | Max `100`. |
| `cursor` | `string` | — | Base64-encoded opaque cursor from a previous response. |

**Response — 200 OK**

```json
{
  "items": [
    {
      "id": "22222222-2222-2222-2222-222222222222",
      "certificationId": "11111111-1111-1111-1111-111111111111",
      "provider": "aws",
      "title": "AWS Certified Cloud Practitioner - Practice Exam 2026-07-28T12:00:00.000Z",
      "status": "READY",
      "createdAt": "2026-07-28T12:00:00.000Z",
      "finishedAt": "2026-07-28T12:00:45.000Z",
      "s3KeyJson": "exams/22222222-2222-2222-2222-222222222222/exam.json",
      "s3KeyPdf": "exams/22222222-2222-2222-2222-222222222222/exam.pdf"
    }
  ],
  "cursor": {
    "nextCursor": null,
    "hasNextPage": false
  }
}
```

`nextCursor` is an opaque base64 string. Pass it as the `cursor` query parameter to fetch the next page. `hasNextPage` indicates whether another page exists.

**Errors**: `400 InvalidRequest` (invalid query parameters).

---

### Get exam status

#### `GET /v1/exams/{id}/status`

Lightweight endpoint for polling. Always returns the current status, even while `GENERATING` or `FAILED`.

**Response — 200 OK**

```json
{
  "id": "22222222-2222-2222-2222-222222222222",
  "status": "GENERATING",
  "createdAt": "2026-07-28T12:00:00.000Z",
  "finishedAt": null
}
```

Status values:

- `GENERATING` — generator is working.
- `READY` — exam is ready for review/download.
- `FAILED` — generation failed; request a new exam.

**Errors**: `400 InvalidRequest` (bad UUID), `404 NotFound`.

---

### Get full exam

#### `GET /v1/exams/{id}`

Returns the full canonical exam JSON including the question set. Only available when `status === "READY"`.

**Response — 200 OK**

```json
{
  "schemaVersion": "1.0.0",
  "id": "22222222-2222-2222-2222-222222222222",
  "certificationId": "11111111-1111-1111-1111-111111111111",
  "provider": "aws",
  "title": "AWS Certified Cloud Practitioner - Practice Exam 2026-07-28T12:00:00.000Z",
  "status": "READY",
  "createdAt": "2026-07-28T12:00:00.000Z",
  "finishedAt": "2026-07-28T12:00:45.000Z",
  "s3KeyJson": "exams/22222222-2222-2222-2222-222222222222/exam.json",
  "s3KeyPdf": "exams/22222222-2222-2222-2222-222222222222/exam.pdf",
  "questions": [
    {
      "id": "33333333-3333-3333-3333-333333333333",
      "number": 1,
      "domain": "Cloud Concepts",
      "difficulty": "medium",
      "text": "Which AWS service provides object storage?",
      "options": [
        { "id": "opt-1", "label": "A", "text": "Amazon S3", "isCorrect": true },
        { "id": "opt-2", "label": "B", "text": "Amazon EC2", "isCorrect": false },
        { "id": "opt-3", "label": "C", "text": "Amazon RDS", "isCorrect": false },
        { "id": "opt-4", "label": "D", "text": "Amazon CloudFront", "isCorrect": false }
      ],
      "explanation": "Amazon S3 is the object storage service.",
      "reference": "https://docs.aws.amazon.com/s3/latest/userguide/Welcome.html"
    }
  ]
}
```

**Errors**: `400 InvalidRequest` (bad UUID), `404 NotFound`, `409 ExamNotReady` (not `READY` yet).

---

### Get PDF download URL

#### `GET /v1/exams/{id}/download`

Returns a time-limited presigned URL for the generated PDF. Only available when `status === "READY"`.

**Response — 200 OK**

```json
{
  "downloadUrl": "https://s3.amazonaws.com/...",
  "expiresAt": "2026-07-28T12:05:00.000Z"
}
```

The URL expires after a short window (default 5 minutes). Fetch it on user action and immediately redirect or start the download.

**Errors**: `400 InvalidRequest` (bad UUID), `404 NotFound`, `409 ExamNotReady`.

---

### Delete an exam

#### `DELETE /v1/exams/{id}`

Deletes the exam metadata row and any S3 artifacts. Returns an empty body on success.

**Response — 204 No Content**

No body.

**Errors**: `400 InvalidRequest` (bad UUID), `404 NotFound`.

---

## Endpoint summary

| Method | Path | Description |
| --- | --- | --- |
| `GET` | `/v1/health` | Health check. |
| `POST` | `/v1/certifications` | Create a certification. |
| `GET` | `/v1/certifications` | List active certifications. |
| `GET` | `/v1/certifications/{id}` | Get a certification. |
| `PUT` | `/v1/certifications/{id}` | Update certification metadata. |
| `POST` | `/v1/exams` | Request a new exam generation. |
| `GET` | `/v1/exams` | List exams with filters and pagination. |
| `GET` | `/v1/exams/{id}` | Get full exam JSON (ready only). |
| `GET` | `/v1/exams/{id}/status` | Poll generation status. |
| `GET` | `/v1/exams/{id}/download` | Get presigned PDF download URL. |
| `DELETE` | `/v1/exams/{id}` | Delete an exam and its artifacts. |
